/**
 * @file
 * Drupal Training behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.drupalTraining = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
